#!/bin/bash

rm -f /var/gem/spec/dummy/tmp/pids/server.pid
bundle check > /dev/null 2>&1 || bundle install -j4

if [ "$#" == 0 ]
then
  bundle exec rake db:migrate
  echo "booting the application..."
  exec bundle exec rails server -u puma -b '0.0.0.0'
fi

exec $@
